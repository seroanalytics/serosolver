% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_antibody_model.R
\name{plot_antibody_predictions}
\alias{plot_antibody_predictions}
\title{Plots model predicted titers against observations}
\usage{
plot_antibody_predictions(
  chain,
  infection_histories,
  antibody_data = NULL,
  demographics = NULL,
  par_tab = NULL,
  antigenic_map = NULL,
  possible_exposure_times = NULL,
  nsamp = 1000,
  measurement_bias = NULL,
  data_type = 1,
  start_level = "none",
  settings = NULL,
  exponential_waning = NULL,
  verbose = FALSE
)
}
\arguments{
\item{chain}{the full MCMC chain to generate antibody level trajectories from, usually `chains$theta_chain`}

\item{infection_histories}{the MCMC chain for infection histories, usually `chains$inf_chain`}

\item{antibody_data}{the antibody data frame, with one row per measurement}

\item{demographics}{optional data frame identifying the demographic group for each individual. This is used when model parameters are stratified by demographics. See the [demographic stratification and covariate vignette](https://seroanalytics.github.io/serosolver/articles/demographics_covariates.html).}

\item{par_tab}{the model control table specifying the parameters in the MCMC chain}

\item{antigenic_map}{(optional) a data frame of antigenic x and y coordinates. Must have column names: x_coord; y_coord; inf_times. The `inf_times` column identifies the circulation or exposure time represented by each map entry. See \code{\link{example_antigenic_map}}}

\item{possible_exposure_times}{(optional) if no antigenic map is specified, this argument gives the vector of times at which individuals can be infected}

\item{nsamp}{number of draws to take from the posterior}

\item{measurement_bias}{default NULL, optional data frame mapping each `biomarker_id` and `biomarker_group` combination to the `rho_index` of the measurement-shift parameter that it uses. See the [advanced features vignette](https://seroanalytics.github.io/serosolver/articles/advanced_features.html).}

\item{data_type}{numeric or text value: `1` or `"discrete"` for discrete, bounded data; `2` or `"continuous"` for continuous, bounded data; or `3` or `"false_positive"` for continuous data with the false-positive observation model. Supply one value per biomarker group, or one value to use for all groups. For bounded data, the limits are given by `min_measurement` and `max_measurement` in par_tab.}

\item{start_level}{`"none"` or a starting-level summary or data frame. A starting level is the antibody level assigned before the modelled infection history begins. With `"none"`, starting levels are set to zero. See the [advanced features vignette](https://seroanalytics.github.io/serosolver/articles/advanced_features.html).}

\item{settings}{if not NULL, list of serosolver settings as returned from the main serosolver function, such as `res$settings`}

\item{exponential_waning}{Deprecated compatibility argument. Prefer a fixed `exponential_waning` row in `par_tab`, with `values = 1` and `par_type = 0`.}

\item{verbose}{if TRUE, prints messages when settings are used or predictions are prepared}
}
\value{
a list with: 
\itemize{
\item a data frame with all posterior estimates for each observation; 
\item the proportion of observations captured by the 95% prediction intervals; 
\item a histogram comparing posterior median estimates to the observed data (note, this can be misleading for continuous data due to the zero-inflated observation model); 
\item a histogram comparing random posterior draws to the observed data (can be more reliable than posterior medians);
\item comparison of observations and all posterior medians and 95% prediction intervals
}
}
\description{
Compares observed antibody measurements with posterior predictions and returns the prediction data and comparison plots.
}
\seealso{
Other infection_history_plots:
\code{\link[=calculate_infection_history_statistics]{calculate_infection_history_statistics()}},
\code{\link[=plot_antibody_data]{plot_antibody_data()}},
\code{\link[=plot_cumulative_infection_histories]{plot_cumulative_infection_histories()}},
\code{\link[=plot_estimated_antibody_model]{plot_estimated_antibody_model()}},
\code{\link[=plot_individual_number_infections]{plot_individual_number_infections()}},
\code{\link[=plot_infection_history_chains_indiv]{plot_infection_history_chains_indiv()}},
\code{\link[=plot_infection_history_chains_time]{plot_infection_history_chains_time()}},
\code{\link[=plot_infection_history_posteriors]{plot_infection_history_posteriors()}},
\code{\link[=plot_model_fits]{plot_model_fits()}},
\code{\link[=plot_total_number_infections]{plot_total_number_infections()}}
}
\concept{infection_history_plots}
